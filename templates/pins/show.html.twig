{% extends 'base.html.twig' %}

{% block title pin.title %}

{% block body %}

<article>
	<h1> {{pin.title}} </h1>
</article>

<p>{{pin.description}}</p>

<a href="{{ path('pins_edit', {'id':pin.id}) }}"> Edit </a>|
<a href="{{ path('pins_home') }}"> Back </a>

{% endblock %}

{% extends 'base.html.twig' %}

{% block title pin.title %}

{% block body %}

<article>
	<h1> {{pin.title}} </h1>
</article>

<p>{{pin.description}}</p>

<a href="{{ path('pins_edit', {'id':pin.id}) }}"> Edit </a>|
<a href="https://google.com" onclick="event.preventDefault(); confirm('Are you sure!') && 
document.getElementById('js-pin-delete-form').submit();"> Delete </a>|
<a href="{{ path('pins_home') }}"> Back </a>

<form id="js-pin-delete-form" action="{{ path('pins_delete', {'id':pin.id}) }}" method="post" style="dysplay:none;">
	<input type="hidden" name="_method" value="DELETE">
</form>

{% endblock %}
